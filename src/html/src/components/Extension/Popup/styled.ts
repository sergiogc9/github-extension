import styled from 'styled-components';
import { Flex } from '@sergiogc9/react-ui';

const StyledExtensionPopup = styled(Flex)``;

StyledExtensionPopup.defaultProps = {
	height: 400,
	overflow: 'hidden',
	flexDirection: 'column',
	width: 800
};

export { StyledExtensionPopup };
