import styled from 'styled-components';
import { Box } from '@sergiogc9/react-ui';

const StyledExtensionPopup = styled(Box)``;

StyledExtensionPopup.defaultProps = {
	height: 400,
	overflow: 'hidden',
	flexDirection: 'column',
	width: 800
};

export { StyledExtensionPopup };
